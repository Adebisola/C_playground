#include <stdio.h>

int main()
{
	/* local variable, an integer which is a whole number */

	int card_count = 11;

	if (card_count > 10)
	{
		/* puts displays a stirng on the terminal */
		puts("The deck is hot. Increase bet!");
	}
	return (0);
}
